#!/bin/sh

set -e
set -x

if [ -n "$(which cygpath)" ] ; then
	srcdir=$(cygpath -u $srcdir)
	builddir=$(cygpath -u $builddir)
fi
export PYTHONPATH="$srcdir/src/mmpack-build:$builddir/src/mmpack-build"

python3 $srcdir/tests/pytests.py
